[gd_scene load_steps=4 format=2]

[ext_resource path="res://src/UI/TransitionsUI.gd" type="Script" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform float threshold = 0.2;

void fragment(){

	float aspect_ratio = SCREEN_PIXEL_SIZE.y/SCREEN_PIXEL_SIZE.x;

	float n_x = aspect_ratio*(UV.x - 0.5);
	float n_y = (UV.y - 0.5); 
	float radius_from_center = sqrt(n_x*n_x + n_y*n_y);

	if (radius_from_center < threshold){
		COLOR.a = 0.0;
	}
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/threshold = 0.2

[node name="TransitionsUI" type="Control"]
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource( 1 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="ColorRect" type="ColorRect" parent="."]
material = SubResource( 2 )
anchor_right = 1.0
anchor_bottom = 1.0
mouse_filter = 2
color = Color( 0, 0, 0, 1 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="Tween" type="Tween" parent="."]
